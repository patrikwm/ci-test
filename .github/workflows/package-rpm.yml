name: Package rpm

on:
  workflow_call:

jobs:
  package-rpm:
    name: "Package rpm"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Building rpm package
        run: |
            echo "Building rpm package"
